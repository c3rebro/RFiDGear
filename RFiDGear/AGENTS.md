# Agent Instructions

- regex search case insensitive the solution for the term **Codex TODO:**. Instructions will be updated for specific tasks for each request, except the prompt says to ignore these tasks. When a task is done, remove those todo lines.
- Preserve and update existing XML documentation comments when modifying C# code.
- Prefer focused, single-responsibility methods; extract helpers instead of adding large conditional blocks.
- Update or add unit/integration test notes in the PR description when changing observable behavior.